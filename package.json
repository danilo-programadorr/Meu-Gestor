{
  "name": "meu-gestor-financeiro-firestore-rules",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test:rules": "firebase emulators:exec --project demo-meu-gestor-financeiro --only firestore \"node --test test/firestore_rules/firestore.rules.test.mjs\"",
    "test:rules:log": "node test/firestore_rules/audit-emulator-log.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.0"
  }
}
